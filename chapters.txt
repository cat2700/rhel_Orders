# =============================== 
            chapter 1
# ===============================
pwd     # to show my current directory
whoami  # to tell me who user am i
cd      # to go back
date
cal     # calender this month
cal 12 1981
cal 2020
ls  # equal dir in windows
ls ~ # show under home dir for current user
ls .    # show under current location
ls -a   # -a is option equal [ls --all]
ls -la  # equal [ls -l -a] merged it
ls --  (tab) #to show all options
ls --help
ls /home    # excute ls in another dir [/home] is argument
ls -la /home    # command , option , argument
ls -R /home # show summery for all fieles in subDir
tty     # tell terminal type
# [ctrl+shift+N] open new black termonal in desktop mode
# [alt+ctrl+F{2..6}] to open new terminal and switch by [alt+<==]
init 3 # to remove desktop view and to return it use [init 5]
clear # to clear terminal of showing data but [reset] cleaar all commands
passwd # chang pass to my account or another [passwd haitham]
which cal # to know the directory file path
file /etc/test # to know type of file is readable or hex
cat /etc/test # to read file content
head /etc/test # to read first 10 line and can use[-n 3] to read just 3 lines
tail /etc/test # to read last 10 line and can use[-n 3] to read just 3 lines
wc /etc/test # to get file summery like lines,words,chars with options [-l , -w , -c]
history (OR (cat .bash_history)) # to show all command from [.bash_history] and to excute command [!5] mean excute the command number 5
cal ; pwd # excute more than command
pwd && ls # ecute ls if excute pwd correctly
pwd || ls # excute ls if pwd is not excute correctly
# =============================== 
            chapter 2
# ===============================
cd  # to go to home dir for current user
cd ..   # up one level
cd ../..   # up two level
cd - # go back
cd ~abeer    # to go to home dir for user abeer
cd Documents #  to open directory
touch file1 file\ 2 'file 5' file5  # create files
mkdir fold1 'fold 4'    # create directories
mkdir -p d1/d2/d3   # make dir and sub dirs [ls -R d1] to show all thim
cp file1 file2 # copy file1 as a file2 on same dir
cp file1 file2 ./Desktop # copy file1 and file2 to another dir
